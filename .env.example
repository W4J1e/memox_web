# 复制为 .env.local 用于本地开发；生产环境在构建平台（如 EdgeOne Makers）的环境变量中配置同名变量。
# 仅 VITE_ 前缀的变量会暴露给前端代码（Vite 约定）。
# 【必需】OneDrive 同步使用的 Azure AD 应用（客户端）ID —— 公开值，无保密要求。
# 并在该应用"身份验证"中把部署域名的裸域（如 https://your-domain.com）注册为重定向 URI。
# 注意：redirect_uri 由代码动态取当前域名（window.location.origin），无需在此配置。
VITE_ONEDRIVE_CLIENT_ID=#https://your-domain.com（不带斜杠）
